/// Paths to SVG icons under assets/icons/.
///
/// Drop your .svg files into assets/icons/ and add an entry here,
/// then use AppIcon(AppIcons.yourIcon) instead of Icons.*.
class AppIcons {
  AppIcons._();

  static const String _base = 'assets/icons';

  // Example — add real entries as svg files are added:
  // static const String home = '$_base/home.svg';
}
